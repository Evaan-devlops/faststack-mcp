
PYTHONPATH=src .venv/bin/python -m pytest -q tests/unit/test_auth_route_playwright.py tests/unit/test_runtime_textbox_playwright.py tests/unit/test_runtime_smoke.py tests/unit/test_playwright_fixture.py


PYTHONPATH=src .venv/bin/python -m pytest -q tests/unit/test_auth_route_playwright.py tests/unit/test_runtime_textbox_playwright.py tests/unit/test_runtime_smoke.py tests/unit/test_playwright_fixture.py





PYTHONPATH=src .venv/bin/python -m pytest -q tests/unit/test_auth_route_playwright.py tests/unit/test_runtime_textbox_playwright.py tests/unit/test_runtime_smoke.py tests/unit/test_playwright_fixture.py


The command failed because the copied text used long typographic dashes (—) instead of normal hyphens (--), and the multiline block was split into separate shell commands.

Run these one line at a time, exactly as written:

PIP_DEFAULT_TIMEOUT=180 PIP_RETRIES=10 .venv/bin/python -m pip install playwright==1.61.0

Wait for it to finish successfully. Then run:

PIP_DEFAULT_TIMEOUT=180 PIP_RETRIES=10 .venv/bin/python -m pip install -e .

Then install Chromium:

PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT=180000 .venv/bin/python -m playwright install chromium

Finally verify:

.venv/bin/python -c "import playwright; print('Playwright package: OK')"

Do not type source and do not recreate .venv.

If the first command again remains at 0.0/42.2 MB, switch to a mobile hotspot or more stable internet and rerun only that first command.


This is a network timeout while downloading the Playwright Python wheel, not a code failure.

Because it is repeatedly showing 0.0/42.2 MB, let the current command finish. If it ends with an error, run this single recovery block:

.venv/bin/python -m pip install \
  --default-timeout=180 \
  --retries 10 \
  playwright==1.61.0

.venv/bin/python -m pip install \
  --default-timeout=180 \
  --retries 10 \
  -e .

PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT=180000 \
.venv/bin/python -m playwright install chromium

If the Playwright wheel still remains at 0.0 MB, switch the Mac to a more stable network or mobile hotspot and rerun the same block. Do not delete .venv again.

After it completes, verify everything in one block:

.venv/bin/python --version
.venv/bin/python -c "import playwright; print('Playwright package: OK')"
.venv/bin/python -m playwright --version

Then we proceed directly to the Mac test gate.


Good—you are inside the cloned repository. First, verify that the Mac has the exact pushed code and working browser environment. Do not run the live OneTrust task yet.

Paste this entire block once:

echo "=== REPOSITORY CHECK ==="
pwd
git branch --show-current
git log -1 --oneline
git status --short

echo
echo "=== PYTHON ENVIRONMENT CHECK ==="
if [ -x ".venv/bin/python" ]; then
  .venv/bin/python --version
else
  echo "MISSING: .venv/bin/python"
fi

echo
echo "=== PLAYWRIGHT CHECK ==="
if [ -x ".venv/bin/python" ]; then
  .venv/bin/python -c "import playwright; print('Playwright Python package: OK')" 2>&1
fi

We expect:

Branch: codex/pre-mac-round-1-gate
Commit beginning: fb419ab
Git status: no unexpected output
.venv Python: available
Playwright Python package: OK

Send the complete terminal output. Then we will run the Mac fixture gate in one block before opening the real site.
